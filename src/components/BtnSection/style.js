import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    containetBtn: {
        backgroundColor: 'red',
        marginVertical: 10,
        width: '100%',
        paddingVertical: 10,
        alignItems: 'center',
        borderRadius: 5,
    },
    labelBtn: {
        color: '#fff',
        marginVertical: 2,
    },
    
})

export default styles